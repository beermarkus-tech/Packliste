export function renderChecklist(container) {
  container.innerHTML = `
    <div class="screen" data-screen="checklist">
      <h2>Checklist</h2>
      <p class="screen-placeholder">Open a trip to check off items per bucket.</p>
    </div>
  `;
}

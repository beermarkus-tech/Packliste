export function renderPrep(container) {
  container.innerHTML = `
    <div class="screen" data-screen="prep">
      <h2>Prep</h2>
      <p class="screen-placeholder">Open a template or trip to assign items to buckets.</p>
    </div>
  `;
}

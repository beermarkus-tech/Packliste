export function renderHome(container) {
  container.innerHTML = `
    <div class="screen" data-screen="home">
      <h2>Home</h2>
      <p class="screen-placeholder">Templates and trips will show up here.</p>
    </div>
  `;
}
